---
title: "Quickstart"
description: "Install and run coding-mcp for remote coding workflows without cloning repositories locally."
---

`coding-mcp` is primarily used when your agent should work on repositories remotely, without cloning source code or doing per-repository setup on the agent machine.

<Info>
  **Prerequisites**:
  - Node.js 20 or newer
  - At least one folder containing project repositories (for example `/projects`)
</Info>

## Install

Install globally from npm:

```bash
npm install -g @kieutrongthien/coding-mcp
```

## Initialize the project registry

Use the CLI to scan and add all projects in a folder:

```bash
cd /my-projects
coding-mcp init
```

or add other projects from another location:

```bash
coding-mcp init /other-projects
```

Add or remove projects later:

```bash
coding-mcp add /projects/my-repo
coding-mcp remove /projects/my-repo
```

## Start the server

HTTP mode:

```bash
coding-mcp serve --transport http --host 0.0.0.0 --port 4000 --mode streamable
```

STDIO mode:

```bash
coding-mcp serve --transport stdio
```

## Next steps

- Configure advanced options in [Configuration](/guides/configuration)
- Explore practical workflows in [Use cases](/guides/use-cases)
- Enable auth in [Security and RBAC](/guides/security-rbac)
- Enable traces in [Observability](/guides/observability)
